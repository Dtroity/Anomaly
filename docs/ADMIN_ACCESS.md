# Доступ администратора к боту

## 🔐 Настройка администратора

### 1. Получение Telegram ID

Чтобы получить свой Telegram ID:

1. Откройте Telegram
2. Найдите бота [@userinfobot](https://t.me/userinfobot)
3. Отправьте команду `/start`
4. Бот вернет ваш Telegram ID (например: `123456789`)

### 2. Добавление ID в конфигурацию

**На Control Server:**

```bash
cd /opt/Anomaly
nano .env
```

Найдите строку `ADMIN_IDS` и добавьте ваш ID:

```env
ADMIN_IDS=123456789
```

Если администраторов несколько, укажите через запятую:

```env
ADMIN_IDS=123456789,987654321,111222333
```

### 3. Перезапуск бота

```bash
docker-compose restart bot
```

Или пересоберите контейнер:

```bash
docker-compose stop bot
docker-compose build bot
docker-compose up -d bot
```

### 4. Проверка доступа

1. Откройте бота в Telegram
2. Отправьте команду `/admin`
3. Если вы видите админ-панель - доступ настроен правильно

## 📋 Команды администратора

### Основные команды

- `/admin` - Открыть админ-панель
- `/grant <telegram_id> <дни> <трафик_gb>` - Выдать бесплатный доступ
- `/ban <telegram_id>` - Заблокировать пользователя
- `/unban <telegram_id>` - Разблокировать пользователя
- `/status <telegram_id>` - Проверить статус пользователя

### Функции админ-панели

1. **👥 Пользователи** - Просмотр списка пользователей
2. **📊 Статистика** - Аналитика и статистика
3. **🎁 Выдать доступ** - Выдача бесплатного доступа
4. **🚫 Заблокировать** - Блокировка пользователей
5. **📢 Рассылка** - Отправка сообщений всем пользователям
6. **🔄 Обновить** - Обновление данных панели

## ⚠️ Важные замечания

1. **Безопасность:**
   - Не передавайте свой Telegram ID третьим лицам
   - Регулярно проверяйте список администраторов
   - Используйте сложные пароли для сервера

2. **Резервное копирование:**
   - Сохраняйте список `ADMIN_IDS` в безопасном месте
   - В случае потери доступа можно восстановить через SSH

3. **Множественные администраторы:**
   - Все администраторы имеют равные права
   - Действия администраторов логируются
   - Проверяйте логи при необходимости

## 🔧 Устранение проблем

### Проблема: Команда `/admin` не работает

**Решение:**
1. Проверьте, что ваш ID добавлен в `ADMIN_IDS`
2. Убедитесь, что ID указан правильно (только цифры, без пробелов)
3. Перезапустите бота
4. Проверьте логи: `docker-compose logs bot | grep admin`

### Проблема: Доступ есть, но панель не открывается

**Решение:**
1. Проверьте подключение к базе данных
2. Проверьте логи бота: `docker-compose logs bot`
3. Убедитесь, что все сервисы запущены: `docker-compose ps`

### Проблема: Нужно добавить нового администратора

**Решение:**
1. Получите Telegram ID нового администратора
2. Добавьте ID в `ADMIN_IDS` через запятую
3. Перезапустите бота
4. Новый администратор должен отправить `/admin` для проверки

## 📞 Поддержка

Если у вас возникли проблемы с доступом администратора:
1. Проверьте логи бота
2. Убедитесь, что конфигурация правильная
3. Обратитесь в поддержку с описанием проблемы

