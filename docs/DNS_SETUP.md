# 🌐 Настройка DNS для Anomaly Connect

## 📋 Необходимые DNS записи

Для домена `anomaly-connect.online` нужно создать следующие записи:

### 1. Основной домен (A запись)

**Для получения SSL сертификата и основного доступа:**

```
Тип: A
Имя: @ (или оставить пустым)
Значение: 72.56.79.212
TTL: 600 (или 3600)
```

### 2. Поддомен для API (A запись)

```
Тип: A
Имя: api
Значение: 72.56.79.212
TTL: 600
```

### 3. Поддомен для панели (A запись)

```
Тип: A
Имя: panel
Значение: 72.56.79.212
TTL: 600
```

## 🔧 Настройка в Timeweb Cloud

### Шаг 1: Создание A записи для основного домена

1. Откройте панель Timeweb Cloud
2. Перейдите в **Домены и SSL** → **DNS**
3. Выберите домен `anomaly-connect.online`
4. Нажмите **"Добавить запись"**
5. Заполните:
   - **Тип:** A
   - **Домен:** anomaly-connect.online (или оставить пустым для корневого)
   - **IP-адрес:** Выберите сервис "Intelligent Raven 72.56.79.212" или введите вручную `72.56.79.212`
   - **TTL:** 600
6. Нажмите **"Добавить"**

### Шаг 2: Создание A записи для api.anomaly-connect.online

1. Нажмите **"Добавить запись"**
2. Заполните:
   - **Тип:** A
   - **Домен:** Введите вручную `api.anomaly-connect.online` или выберите из списка и добавьте префикс `api`
   - **IP-адрес:** 72.56.79.212
   - **TTL:** 600
3. Нажмите **"Добавить"**

### Шаг 3: Создание A записи для panel.anomaly-connect.online

1. Нажмите **"Добавить запись"**
2. Заполните:
   - **Тип:** A
   - **Домен:** `panel.anomaly-connect.online`
   - **IP-адрес:** 72.56.79.212
   - **TTL:** 600
3. Нажмите **"Добавить"**

## ⏱️ Время распространения DNS

После создания записей:
- **TTL 600:** изменения вступят в силу через 10 минут
- **TTL 3600:** изменения вступят в силу через 1 час

Проверить распространение DNS можно через:
- https://dnschecker.org/
- `nslookup api.anomaly-connect.online`
- `dig api.anomaly-connect.online`

## ✅ Проверка DNS записей

### Через командную строку:

```bash
# Проверка основного домена
nslookup anomaly-connect.online

# Проверка поддомена API
nslookup api.anomaly-connect.online

# Проверка поддомена панели
nslookup panel.anomaly-connect.online
```

Все должны возвращать IP: `72.56.79.212`

### Через онлайн сервисы:

- https://dnschecker.org/
- https://www.whatsmydns.net/

## 🔐 После настройки DNS

После того, как DNS записи распространятся:

1. **Получите SSL сертификат** (см. `setup-ssl.sh`)
2. **Обновите .env файл** с HTTPS URL
3. **Перезапустите сервисы**

---

**Важно:** Не настраивайте SSL до того, как DNS записи полностью распространятся!

