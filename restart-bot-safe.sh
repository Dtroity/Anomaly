#!/bin/bash

# Безопасный скрипт для перезапуска бота с исправлением ContainerConfig

PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$PROJECT_DIR"

echo "🔄 Безопасный перезапуск бота"
echo "=============================="
echo ""

# 1. Удалить поврежденный контейнер бота напрямую
echo "🧹 Удаление поврежденного контейнера бота..."
docker ps -a | grep -E "anomaly-bot" | awk '{print $1}' | xargs -r docker rm -f 2>/dev/null || true
docker-compose rm -f bot 2>/dev/null || true
echo "  ✅ Контейнер удален"
echo ""

# 2. Обновить код
echo "📥 Обновление кода..."
git pull
echo ""

# 3. Пересобрать и запустить бота
echo "🔨 Пересборка и запуск бота..."
docker-compose build --no-cache bot
docker-compose up -d --no-deps bot
echo ""

# 4. Проверить статус
echo "⏳ Ожидание запуска (10 секунд)..."
sleep 10
echo ""

echo "📊 Статус контейнера:"
docker-compose ps bot
echo ""

echo "📋 Логи бота (последние 20 строк):"
docker-compose logs --tail=20 bot
echo ""

echo "✅ Готово!"

